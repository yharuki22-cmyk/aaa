# -*- coding: utf-8 -*-
"""図面写真「配置図兼1階平面図 S=1:100」から読み取った寸法・構成データ。

座標系: 敷地南西角を原点 (0,0)、単位 mm、北が +Y。
値はすべて写真の寸法文字から採ったもの。読み取れなかった位置は
910mm モジュールから推定しており、コメントに (推定) と記す。
"""

# ---- 敷地 ----
SITE_W = 11_000   # 隣地境界線(北・南)の寸法値
SITE_D = 12_500   # 隣地境界線(西)・道路境界線(東)の寸法値

# ---- 通り芯 ----
# X方向: い〜り 9本 @910。「り」通りから道路境界線まで 2,000
GRID_PITCH = 910
X_NAMES = ["い", "ろ", "は", "に", "ほ", "へ", "と", "ち", "り"]
X_RI = SITE_W - 2_000               # り = 9,000
X0 = X_RI - GRID_PITCH * 8          # い = 1,720
GX = {n: X0 + i * GRID_PITCH for i, n in enumerate(X_NAMES)}

# Y方向: 0〜6 7本 @910 (0→6 で 5,460)。
# 右側寸法 2,730(0通り〜敷地南側の基準)より 0通り y=2,730
Y0 = 2_730
GY = {i: Y0 + i * GRID_PITCH for i in range(7)}

# ---- 建物 ----
# 西側ブロック: い〜と × 0〜6 / 東側ブロック(玄関・ホール): と〜り × 1〜5
MAIN_BLOCK = (GX["い"], GY[0], GX["と"], GY[6])
EAST_BLOCK = (GX["と"], GY[1], GX["り"], GY[5])

# 部屋: (x1, y1, x2, y2, 表示名)
ROOMS = [
    (GX["い"], GY[5], GX["は"], GY[6], "便所"),
    (GX["い"], GY[3], GX["は"], GY[5], "浴室"),
    (GX["は"], GY[4], GX["ほ"], GY[6], "洗面\n洗濯室"),
    (GX["ほ"], GY[4], GX["へ"], GY[6], "階段"),        # (推定)幅910
    (GX["へ"], GY[4], GX["と"], GY[5], "物入"),
    (GX["と"], GY[3], GX["り"], GY[5], "玄関"),
    (GX["と"], GY[1], GX["り"], GY[3], "ホール"),
]
LDK_LABEL = ((GX["い"] + GX["と"]) / 2, (GY[0] + GY[3]) / 2, "LDK")

# ---- 外構 ----
TERRACE_DEPTH = 1_000     # 左側寸法 1,000
TERRACE = (GX["い"], GY[0] - TERRACE_DEPTH, GX["へ"], GY[0], "テラス")
PORCH = (GX["と"], GY[5], GX["り"], GY[6], "ポーチ")

# 駐車場: 北側境界から 2,500 の帯に配置 (南北位置は右側寸法 2,500、
# 東西位置は写真からの推定)
PARKING = (GX["に"], SITE_D - 2_500, GX["ち"], SITE_D - 800, "駐車場")

FENCE_NOTE = "アルミフェンス H=900"   # 北側境界沿い

# 植栽 (位置・径は写真からの目測): (x, y, r, 実線か)
TREES = [
    (3_050, 1_800, 600, True), (3_950, 1_950, 620, True), (4_800, 1_750, 580, True),
    (5_400, 1_100, 900, False), (7_300, 1_500, 950, False), (9_400, 1_400, 900, False),
]

# ---- 寸法 (写真に書かれている値のとおり) ----
# 上部: い→は→ほ→と→り 各1,820 / 下部: い→ろ 910, ろ→と 4,550, と→り 1,820,
# 全体 7,280, り→道路境界 2,000 / 左: 0→6 で 910+1,820+910+1,820=5,460,
# テラス 1,000, 敷地 12,500 / 右: 北境界→駐車場 2,500, 0通り基準 2,730 ほか

TITLE = "配置図兼1階平面図  S=1:100"
